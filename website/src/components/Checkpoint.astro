---
interface Props { title?: string; }
const { title = 'What success looks like' } = Astro.props;
---
<aside class="rb-checkpoint" aria-label={title}>
  <p class="rb-checkpoint-label"><span aria-hidden="true">✓</span> {title}</p>
  <div><slot /></div>
</aside>
